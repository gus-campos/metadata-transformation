{
  "dependencies": {
    "vitest": "^4.1.5",
    "zod": "^4.4.1"
  },
  "scripts": {
    "test": "vitest",
    "build": "esbuild src/factory.ts --bundle --platform=node --format=cjs --outfile=dist/transformer.js",
    "dev": "esbuild src/factory.ts --bundle --platform=node --format=cjs --outfile=dist/transformer.js --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "license": "UNLICENSED"
}
